# --- Training / notebook (Colab or local GPU) ---
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0
datasets>=2.14.0
seqeval>=1.2.2
Pillow>=9.0.0
matplotlib>=3.5.0
numpy>=1.23.0
pytesseract>=0.3.10   # OCR for inference on raw (non-FUNSD) documents

# --- Serving (FastAPI app in app/) ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9   # required by FastAPI for UploadFile
